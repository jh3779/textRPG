/*
 * Player.cpp
 *
 * 📚 학습 개념:
 * - 생성자 구현: 객체가 생성될 때 초기값 설정
 * - 멤버 함수 구현: ClassName::functionName() 형식
 * - const 함수: 데이터를 수정하지 않는 함수
 *
 * 💡 팁:
 * - this->variable로 멤버 변수에 접근 가능 (보통 생략 가능)
 * - 생성자는 반환값이 없음 (void도 명시하지 않음)
 */

#include "../include/Player.h"
#include <iostream>

 // 💡 생성자 구현 예시:
 // 객체가 생성될 때 이 코드가 실행됨
 // Player(const std::string& playerName) {
 //     name = playerName;
 //     hp = 100;
 //     maxHp = 100;
 //     attack = 10;
 //     defense = 5;
 //     level = 1;
 //     experience = 0;
 //     gold = 0;
 // }

 // 💡 Getter 함수 구현 예시:
 // std::string getName() const {
 //     return name;
 // }

 // 👇 여기에 나머지 함수들의 구현 코드를 작성하세요
