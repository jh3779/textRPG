/*
 * Utils.cpp
 *
 * 📚 학습 개념:
 * - 함수 구현: 헤더에서 선언한 함수의 실제 코드 작성
 * - 반복문(while, for): 조건이 참일 때 반복 실행
 * - 조건문(if): 특정 조건에 따라 다른 코드 실행
 *
 * 💡 팁:
 * - 헤더 파일에서 선언한 함수와 같은 시그니처(이름, 매개변수, 반환값)여야 함
 * - :: (스코프 연산자)는 어느 namespace/클래스의 함수인지 명시
 */

#include "../include/Utils.h"
#include <thread>
#include <chrono>

 // 💡 namespace 사용: Utils::로 시작하는 함수들임을 명시
namespace Utils {

    // 👇 여기에 함수들의 구현 코드를 작성하세요

    // 예시:
    // int getValidInput(int minValue, int maxValue) {
    //     int input;
    //     while (true) {
    //         std::cin >> input;
    //         if (input >= minValue && input <= maxValue) {
    //             return input;
    //         }
    //         std::cout << "잘못된 입력입니다. " << minValue << "~" << maxValue << " 사이의 숫자를 입력하세요." << std::endl;
    //     }
    // }

    int getValidIput(int minValue, int maxValue) {

    }
}
