# 06. 미해결 질문 · 가정 · 결정 로그

> 인터뷰 영역 8 + 진행 중 파생 · 상태: living document

## Open Questions (미결정)
| ID | 질문 | 영향 | 담당 | 필요 시점 |
|----|------|------|------|-----------|
| OQ-001 | 서버 API의 정확한 엔드포인트/요청-응답 스키마(REST 경로, 세션 토큰 전달 방식 등) | 04_data_model, 구현 착수 | 우지훈/구현 세션 | design-generation 단계 착수 전 |
| OQ-002 | 새 게임 시작 시 기존 localStorage 세이브를 덮어쓸 때 확인 모달을 띄울지, 그냥 덮어쓸지 | SCR-001, FLOW-001 | 우지훈 | 화면 설계(design-generation) 시점 |
| OQ-003 | 배포 방식(로컬 실행만 vs 실제 공개 배포, 배포한다면 프론트/서버 호스팅 위치) | 배포 전략, REQ-NF-002 | 우지훈 | MVP 구현 완료 후 |

## 가정 (Assumptions)
| ID | 가정 | 근거/리스크 |
|----|------|-------------|
| ASM-001 | 사용자는 로그인 없이 브라우저 하나로만 플레이한다(멀티 기기 이어하기 불필요) | 개인 학습 프로젝트 규모 — 리스크 낮음 |
| ASM-002 | 서버는 항상 켜져 있다고 가정(오프라인 지원 없음) | REQ-NF-002. 배포 방식(OQ-003)에 따라 재검토 가능 |
| ASM-003 | 게임 밸런스(수치·확률)는 콘솔 버전과 1:1 동일하게 유지, 웹이라서 별도 조정하지 않는다 | 00_project_brief "핵심 가치"와 직결 |

## 결정 로그 (DEC-*)
| ID | 결정 | 근거 | 날짜 |
|----|------|------|------|
| DEC-001 | 웹 버전은 C++ 게임 로직을 서버로 노출하는 방식으로 구현한다(로직 재작성/WASM 컴파일 대신) | 로직 이중관리 방지, 기존 코드 재사용 극대화 | 2026-08-18 |
| DEC-002 | 서버-클라이언트 통신은 REST HTTP로 한다(WebSocket 불필요) | 턴제 요청-응답 구조라 실시간 스트리밍이 필요 없음 | 2026-08-18 |
| DEC-003 | 저장/불러오기는 기존 `saves/save1.txt`의 필드 구조를 그대로 JSON으로 직렬화해 localStorage에 둔다 | 콘솔 버전과의 포맷 일관성, 재사용 | 2026-08-18 |
| DEC-004 | MVP는 콘솔 버전 전체 기능(AI 던전마스터 제외)을 포함한다 — 부분 슬라이스로 나누지 않는다 | 이미 완성된 게임 로직을 재사용하므로 축소할 이유가 적음 | 2026-08-18 |
| DEC-005 | AI 던전마스터(`ai_service`) 연동은 이번 웹 MVP 범위에서 제외한다 | 스코프 통제 — 먼저 정적 텍스트 기반 웹 플레이를 완성 | 2026-08-18 |

## 리스크
| 리스크 | 심각도 | 완화책 |
|--------|:------:|--------|
| C++ 로직을 서버로 노출하는 구체적 방법(별도 HTTP 서버 프로세스로 감쌀지, 기존 Game 클래스를 API 레이어에서 직접 호출할지)이 아직 설계되지 않음 | 중간 | design-generation 이전에 별도 아키텍처 설계(EnterPlanMode)로 확정 — OQ-001과 연결 |
| 콘솔 텍스트 출력(`std::cout` 직접 호출)이 곳곳에 흩어져 있어, API 응답으로 바꾸려면 출력부와 로직부 분리 리팩터링이 선행되어야 함 | 중간 | 구현 단계에서 별도 리팩터링 작업으로 처리(이 문서의 범위 밖) |
